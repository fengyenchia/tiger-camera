# Assets

這個目錄只保存專案共用、已確認授權且不屬於特定 Web bundle 的原始資產說明。Web 實際使用的字型與圖片放在 `web/frontend/public/`，避免複製兩份。

規則：

- 所有 Web 文字統一使用 `NotoSansTC-VF.ttf` 與專案既有 `font-title` 設定。
- 不加入喇叭、虎叫聲或拍後隨機文字素材。
- 不提交無授權字型、圖片、音訊或第三方 3D 檔。
- 新增素材時記錄來源、作者、授權與修改方式。
